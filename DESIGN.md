# Video Motion Design Guide

이 문서는 이 루트 폴더의 모든 Remotion, HyperFrames 영상 작업의 시각 기준이며, White Animation에는 여백·가독성·싱크·검수 안전 규칙으로만 적용합니다. White Animation의 시각 기준은 `srt-whiteboard-animation` 레포의 자체 가이드이며, 별도 외부 `DESIGN.md`를 요청하지 않습니다.

## 기본 목적

음성이나 스크립트의 핵심 메시지를 짧은 텍스트와 인포그래픽 모션으로 이해시키는 영상을 만든다.

텍스트는 내용을 전부 받아쓰는 자막이 아니라, 장면의 의미를 고정하는 표지판 역할을 한다. 인포그래픽은 텍스트를 보조하는 장식이 아니라 메시지 구조를 보여주는 본체다.

## 화면 톤

- 깨끗한 업무용 브리핑 느낌
- 밝은 캔버스, 진한 잉크 라인, 절제된 포인트 컬러
- 슬라이드 템플릿처럼 정적인 화면보다 흐름도, 분기, 처리 파이프라인, 체크리스트 같은 정보 구조 중심
- 과한 장식, 그라데이션 히어로, 반복 카드 그리드 지양
- 특정 도구 로고에 의존하지 않고, 범용적인 아이콘과 구조로 설명

## 화면 구성과 자막 안전영역

- 인포그래픽 중심 레퍼런스처럼 장면의 주역은 큰 인포그래픽이어야 한다. 제목과 짧은 보조 문구는 인포그래픽을 여는 역할만 하고, 설명은 아이콘, 선, 카드, 노드, 화살표, 단계 흐름으로 전달한다.
- 기본 구조는 상단의 짧은 제목/설명, 중앙의 넓은 인포그래픽 본체, 하단의 자막 안전영역으로 나눈다.
- 하단 자막 안전영역은 기본적으로 화면 높이의 18-22%를 비워둔다. 1920x1080 기준으로는 하단 약 200-240px 안에 핵심 인포그래픽, 라벨, 장식, 진행 막대가 들어오지 않게 한다.
- 자막이 2줄까지 들어갈 수 있도록 하단에는 충분한 빈 공간과 단순한 배경을 유지한다. 자막 영역 위쪽 경계와 인포그래픽 사이에도 여유를 둔다.
- 장면 라벨은 자막 영역에 두지 않는다. 필요한 라벨은 인포그래픽 내부의 명확한 위치, 상단 설명 아래, 또는 측면 트레이에 배치하되 자막 영역과 겹치지 않게 한다.
- 검수용 프레임에는 자막 안전영역 가이드를 표시해 확인할 수 있지만, 최종 렌더에는 가이드, 시간표시, 진행률 바가 남지 않아야 한다.

## 색상

- Canvas: `#F4F7F5`
- Ink: `#17201D`
- Muted ink: `#60716B`
- Structure teal: `#0E8F88`
- Warning coral: `#E45A3D`
- Insight mustard: `#E9B44C`
- Deep violet accent: `#5157C9`

작업 주제에 맞춰 색상은 조정할 수 있지만, 한 가지 색 계열만 반복하는 팔레트는 피한다.

## 박스 경계 렌더 안전성

- 이 규칙은 모든 모션그래픽 작업의 카드, 노드, 칩, 표 컨테이너, 다이어그램 박스에 적용한다. White Animation은 자체 시각 가이드를 따르되, 박스를 사용할 경우 이 가독성 안전 규칙을 적용한다.
- `1px`의 옅은 선이나 검정 계열의 가는 선만으로 박스 경계를 구분하지 않는다. H.264 인코딩과 축소 재생에서 선이 사라지거나 탁하게 보일 수 있다.
- 기본 박스 경계는 배경과 구별되는 중간 명도의 색으로 `2px` 이상 사용한다. 현재 단계·선택·경고처럼 중요한 상태는 기본 경계보다 한 단계 강한 색과 `3px` 이상으로 표시한다.
- `1px` hairline은 카드 내부 구분선, 배경용 가이드, 표의 보조선처럼 경계가 사라져도 의미가 유지되는 곳에만 사용한다.
- 그림자, 채우기, 애니메이션이 있더라도 박스의 유일한 식별 수단으로 삼지 않는다. 정지 프레임에서도 경계와 내용 영역을 구분할 수 있어야 한다.
- QA에서는 최종 MP4 또는 동등한 압축 샘플을 50% 크기로 확인해, 일반·활성 박스의 테두리가 끊기거나 배경에 묻히지 않는지 검수한다.

## 타이포그래피

- Primary: `Pretendard`, `IBM Plex Sans KR`, `Malgun Gothic`, sans-serif
- 전체 음성을 자막처럼 계속 노출하지 않는다.
- 장면마다 핵심 제목, 1줄 설명, 짧은 라벨을 사용한다.
- 큰 제목은 1개, 보조 문구는 1개, 라벨은 장면당 2-4개를 기준으로 한다.
- 텍스트는 인포그래픽을 가리지 않아야 한다.
- 모바일/데스크톱 어느 크기에서도 텍스트가 박스 밖으로 넘치지 않도록 줄바꿈과 크기를 관리한다.

## 텍스트 줄바꿈

- 제목과 핵심 문구는 브라우저 자동 줄바꿈에만 맡기지 않고 의미 단위로 줄을 나눈다.
- 한국어 제목에서 한 글자, 조사, 어미, 짧은 술어 어절만 다음 줄에 남기지 않는다.
- 예: `아이디어는 있는데 영상에서 멈춘다면`은 `아이디어는 있는데` / `영상에서 멈춘다면`처럼 나눈다.
- `멈춘다면`, `시작하려면`, `필요합니다` 같은 술어 어절은 단독으로 쪼개지 않는다. 공간이 부족하면 앞 어절과 함께 다음 줄로 보낸다.
- 제목 영역은 `word-break: keep-all`, `overflow-wrap: normal`을 우선하고, 필요하면 제목 컨테이너 폭, 폰트 크기, 행간을 조정한다.

## 인포그래픽 원칙

- 텍스트 중심 슬라이드보다 인포그래픽 중심 장면을 우선한다.
- 장면마다 서로 다른 구조를 사용한다.
- 같은 카드 배열이나 같은 차트 패턴을 반복하지 않는다.
- 스크립트의 논리 흐름과 화면 구조가 맞아야 한다.
- 문제 제기는 경고, 분기는 맵/노드, 프로세스는 파이프라인, 비교는 전후 구조, 결론은 체크/수렴 구조처럼 의미에 맞는 형태를 고른다.
- 라벨은 도형의 의미를 바로 알 수 있게 붙인다.
- 인포그래픽 본체가 하단 자막 안전영역을 침범하면 본체를 축소하거나 위로 재배치한다.

## 발화 순서 기반 시각 완성

- 장면 이미지는 장면 시작에 한꺼번에 완성하지 않는다. 핵심 요소는 내레이션의 주장·근거·사례·결론 순서에 맞춰 등장하고 연결되며 완성된다.
- 각 발화 구절은 최소 하나의 구체적인 시각 변화와 연결한다. 단순한 아이콘 교체나 같은 카드 흐름의 반복만으로 서로 다른 내용을 표현하지 않는다.
- 해당 발화의 결론이나 다음 단계는 그 발화가 설명되기 전에 노출하지 않는다. 관련 발화가 끝날 즈음 핵심 구조를 완성하고, 최소 0.5초 동안 읽을 수 있게 유지한다.
- `카드 → 연결선 → 다음 카드`는 단계·파이프라인에 적합한 공개 순서다. 문제 제기는 단절 맵, 관계 설명은 관계망, 비교는 전후 구조, 승인 판단은 게이트, 개선은 순환 루프, 역할 설명은 분기 구조처럼 메시지에 맞는 시각 문법을 사용한다.

## 필요한 텍스트 모션그래픽

- 그림만으로 전문용어, 수치, 대조 기준, 결론을 정확하게 전달하기 어려운 경우에는 짧은 제목·키워드·라벨을 독립 모션그래픽 요소로 사용한다.
- 텍스트는 해당 발화 시작에 맞춰 등장하고, 관계선·카드·강조 영역과 함께 의미를 완성한다. 전체 내레이션을 계속 받아쓰는 자막으로 사용하지 않는다.
- 기본 밀도는 장면당 제목 1개와 짧은 라벨 2–4개다. 텍스트는 인포그래픽을 가리지 않으며, 하단 자막 안전영역 대신 상단 또는 측면 트레이에 둔다.
- White Animation의 원화 파일은 무문자로 유지한다. 필요한 텍스트는 원화와 분리된 렌더 단계의 오버레이로 적용해, 선화·annotation·손글씨 효과와 독립적으로 타이밍을 제어한다.

## 인물 서사와 손그림 캐릭터

- 사람의 역할, 판단, 협업, 행동이 메시지의 핵심이면 인물을 단순 막대 아이콘·범용 실루엣·작은 장식으로 처리하지 않는다. 사진이나 3D 대신 역할을 읽을 수 있는 손그림 캐릭터를 사용한다.
- 캐릭터는 최소 두 가지 이상의 식별 단서를 가진다. 예: 안전모·조끼 같은 복장, 클립보드·도면·태블릿 같은 도구, 회의·점검·기록·검토 같은 자세와 행동, 현장·사무실·회의 테이블 같은 상황.
- 같은 역할은 장면 사이에서 일관된 외형을 유지하고, 서로 다른 역할은 복장·도구·배치로 구분한다. 인물이 핵심 주체인 장면에서는 데이터 카드나 추상 아이콘보다 인물의 행동이 먼저 읽혀야 한다.
- White Animation의 원화는 무문자·비사진 원칙을 유지한다. 인물의 이름, 역할명, 전문용어가 필요하면 원화와 분리된 짧은 텍스트 오버레이로 보완한다.

## 장면 설계 순서

1. 스크립트를 6-12개 장면으로 나눈다.
2. 각 장면마다 핵심 메시지 1개를 정한다.
3. 핵심 메시지를 제목 1개, 보조 설명 1개, 라벨 2-4개로 압축한다.
4. 앞뒤 장면과 중복되지 않는 인포그래픽 구조를 고른다.
5. 하단 자막 안전영역을 먼저 예약하고, 인포그래픽과 라벨을 그 위의 영역 안에서 완결한다.
6. 대표 프레임을 렌더해 텍스트 겹침, 자막 영역 침범, 여백, 장면 의미를 확인한다. 최종 MP4 또는 동등한 압축 샘플에서는 박스 경계의 두께·명암 대비도 확인한다.
7. 핵심 발화의 시작·중간·종료 프레임을 확인해 발화 순서와 이미지 완성 순서가 같은지 확인한다.
8. 최종 렌더 전 오디오 길이와 영상 길이가 맞는지 확인한다.

## 하지 말 것

- 전체 음성 자막을 화면에 계속 노출하지 않는다.
- 텍스트만으로 설명하는 슬라이드형 화면을 만들지 않는다.
- 같은 카드 레이아웃을 반복하지 않는다.
- 카드, 노드, 칩, 다이어그램 컨테이너에 `1px` hairline만 적용해 경계가 렌더 후 사라지게 하지 않는다.
- 인포그래픽과 라벨이 서로 겹쳐 의미를 가리지 않게 한다.
- 하단 자막 안전영역에 인포그래픽, 라벨, 장식, 검수용 진행 막대를 배치하지 않는다.
- 어두운 배경, 과한 블러, 장식용 오브, 보라/파랑 그라데이션 중심 화면을 기본 스타일로 쓰지 않는다.
